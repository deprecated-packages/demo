services:
# also sets for better code
# vendor/bin/rector p src --dry-run --level code-quality

# also sets for instant upgrades
# vendor/bin/rector p src --dry-run --level php71
# vendor/bin/rector p src --dry-run --level php72
# vendor/bin/rector p src --dry-run --level php73
# vendor/bin/rector p src --dry-run --level php74
