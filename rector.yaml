parameters:
    php_version_features: '7.3'

# demo
# 1. php
# vendor/bin/rector process src --level php71 -n
# vendor/bin/rector process src --level php72 -n
# not only improve, but also fix BC breaks
# vendor/bin/rector process src --level php74 -n

# 2. code quality
# vendor/bin/rector process src --level code-quality -n


# vendor/bin/rector process src --level type-declaration -n


# 3. dead code
# vendor/bin/rector process src --level dead-code -n

# 4. twig
# vendor/bin/rector process src --level twig-underscore-to-namespace -n
# vendor/bin/rector process src --level coding-style -n

# 5. phpunit
# vendor/bin/rector process src --level phpunit70 -n

# 6. CI
# vendor/bin/rector levels


# @todo is that black magic? Not really - show MyFirstRector
#services:
#    Utils\Rector\MyFirstRector: ~